{
  "name": "jpeg-encoder-monorepo",
  "private": true,
  "version": "2.1.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "dev": "pnpm --filter @jpeg-encoder/web dev"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "pnpm": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.1.0"
}