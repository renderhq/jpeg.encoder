{
  "name": "jpeg-encoder-monorepo",
  "private": true,
  "version": "2.1.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "dev": "pnpm --filter @jpeg-encoder/web-demo dev"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "pnpm": "^9.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}